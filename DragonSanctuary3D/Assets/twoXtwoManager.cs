using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class twoXtwoManager : MonoBehaviour
{
    public bool isSomethingPlacedOn;

    // Start is called before the first frame update
    void Start()
    {
        isSomethingPlacedOn = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
